Screenshots referenced by the top-level README. PNG, real data is fine (percentages only).

- `menu-bar-macos.png` — the menu bar item with both quotas visible, cropped tight (2× Retina).
- `popover-macos.png` — the open popover with session + weekly cards, overlays on.
- `tray-windows.png` — the Windows taskbar corner with the icon and the popover open above it.

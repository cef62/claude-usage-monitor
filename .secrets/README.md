Backup of the updater signing keypair (minisign). Never commit. Same files live in ~/.tauri/
and, as repo secrets, TAURI_SIGNING_PRIVATE_KEY / TAURI_SIGNING_PRIVATE_KEY_PASSWORD.
Note: `git clean -fdx` deletes this directory.

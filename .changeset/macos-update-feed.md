---
"claude-usage-monitor": patch
---

macOS installs did not see the 0.14.0 update: its update feed listed only Windows. This release is offered to macOS again; the release build now runs the platforms one after the other and refuses to publish an incomplete feed.
